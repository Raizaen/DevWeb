//var 
var nom = ...; //type automatique

//dom
var balId = window.document.getElementById("JeanJacques"); // stockage de la balise avec l'id jeanjacques 
var tabBalClass = window.document.getElementByClassName("Rousseau"); // stock tab contient toutes les balises qui ont class Rousseau

var newBal = window.document.createElement("div"); // crée balise div 

window.document.removeChild(elementDOM); // Remove an HTML element
body.appendChild(elementDOM); // Add an HTML element
